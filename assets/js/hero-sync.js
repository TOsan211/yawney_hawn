// Curtain scroll effect disabled for now -- see style.scss's .hero-top
// comment. This file is kept (and still loaded from home.html) as a no-op
// so the curtain can be re-enabled later without touching markup.
